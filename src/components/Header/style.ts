import styled from 'styled-components';

export const Container = styled.header`
  display: flex;
  justify-content: center;
  width: 100%;
  padding: 40px 0;

  h1 {
    font-weight: 800;
    color: #1f1f1f;

    span {
      font-weight: 400;
    }
  }
`